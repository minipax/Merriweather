@model MvcMusicStore.Models.Webpage

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Webpage</legend>

    <div class="display-label">Name</div>
    <div class="display-field">@Model.Name</div>

    <div class="display-label">PageContent</div>
    <div class="display-field">@Model.PageContent</div>

    <div class="display-label">IsPublished</div>
    <div class="display-field">@Model.IsPublished</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


